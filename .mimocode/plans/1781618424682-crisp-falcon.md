# 删除 project/user 下防御性代码

## 背景
29 处防御性代码经分析均不可能触发（调用链已保证、枚举已穷举、状态已约束）。删除后减少代码噪声，使真正可触发的路径更清晰。

## 分批策略
按文件分组，每批 2-3 个文件，每批改完后 Keil 编译验证 0 Error / 0 Warning。

---

## 第 1 批：switch default 分支（11 处）

### `executor.c:344` — `executor_state_name()` 删除 `default: return "Unknown";`
### `screen.c:713` — `executor_state_text()` 删除 `default: return "?";`
### `screen.c:726` — `executor_error_text()` 删除 `default: return "E:?";`
### `menu_key.c:21` — `short_event_from_index()` 删除 `default: return MENU_KEY_EVENT_NONE;`
### `menu_key.c:33` — `long_event_from_index()` 删除 `default: return MENU_KEY_EVENT_NONE;`
### `settings.c:248` — `save_state_name()` 删除 `default: return "Empty";`
### `settings.c:262` — `flash_state_name()` 删除 `default: return "Empty";`
### `motion_math.c:170` — `command_to_velocity()` 删除 `case MOTION_STOP: default: break;`（改为空 switch 不含 default）
### `drive_test.c:80` — `drive_square_test_apply_step()` 删除 `default: stop_motion(); break;`
### `drive_test.c:114` — `drive_square_test_target()` 删除 `default: ...`
### `drive_test.c:143` — `drive_square_test_pose_arrived()` 删除 `default: return 1u;`

## 第 2 批：NULL 指针检查（10 处）

### `executor.c:135` — `executor_start()` 删除 `if (waypoints == NULL || count == 0)` 分支
### `art_replan.c:28` — `art_replan_update_reset()` 删除 `if(0 == update)` 检查
### `art_replan.c:55` — `art_replan_begin()` 删除 `if(0 != update)` 条件，直接执行 update 赋值
### `art_replan.c:340` — `art_replan_confirm_launch()` 删除 `if(0 == art_launch_pending)` 检查
### `openart_uart.c:295-306` — `openart_find_player_cell()` 删除三个 `if(0 != row/col/count)` 条件
### `map_utils.c:80` — `map_scan_stats()` 删除 `if(0 == source)` 检查
### `screen.c:355` — `action_step_from_waypoint_step()` 删除 `if((0 == result) || ...)` 中的 NULL 部分
### `screen.c:370` — `waypoint_is_push()` 删除 `(0 != wp) &&` 条件
### `screen.c:378` — `action_step_for_execute_preview()` 删除 `if((0 == result) || ...)` 中的 NULL 部分
### `menu.c:393` — `apply_art_replan_update()` 删除 `if(0 == update)` 检查

## 第 3 批：边界检查（7 处）

### `base_io.c:181` — `set_wheel_pwm_with_deadband()` 删除 `if(wheel >= WHEEL_COUNT)` return
### `drive_test.c:289` — `test_wheel()` 删除 `if(wheel >= WHEEL_COUNT)` return
### `settings.c:136` — `settings_set_runtime()` 删除 `if(mode >= RUN_MODE_COUNT)` 修正分支
### `settings.c:152` — `settings_set_source()` 删除 `if(source >= MAP_SOURCE_COUNT)` 修正分支
### `settings.c:216` — `mode_name()` 删除 `if(mode >= RUN_MODE_COUNT)` 修正分支
### `settings.c:231` — `source_name()` 删除 `if(source >= MAP_SOURCE_COUNT)` 修正分支
### `maps.c:69` — `map_get()` 删除 `if(index >= count)` 修正分支

## 第 4 批：冗余状态恢复（2 处）

### `menu.c:991` — `draw_current_page()` 删除 `if(page->id != current_page)` 恢复逻辑
### `solver.c:457` — `apply_path_to_runtime()` 删除 `if(box != map->targets[...])` 检查

---

## 验证步骤

每批修改后：
1. Keil 编译：`D:\Keil_v5\UV4\UV4.exe -b "C:\Users\ye\Desktop\rt1064\project\mdk\rt1064.uvprojx"` — 确认 0 Error(s), 0 Warning(s)
2. 如有 Warning，逐一修复（可能需要加 `(void)` 或调整语法）

最终：
3. `git diff` 检查所有改动是否准确
4. 板载测试：菜单切换、求解、执行、ART 同步各路径功能正常
